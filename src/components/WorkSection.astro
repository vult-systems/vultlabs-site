---
interface Props {
  title: string;
  description: string;
  variant: string;
  flip?: boolean;
  href?: string;
}

const { title, description, variant, flip = false, href } = Astro.props;
---

<section class="section">
  <div class:list={["grid", "two", { flip }]}>
    {flip ? (
      <Fragment>
        <div class="copy">
          <div class="kicker">Work</div>
          <h3>{href ? <a href={href}>{title}</a> : title}</h3>
          <p class="section-desc">{description}</p>
          <a class="arrow" href={href || "#contact"}><svg viewBox="0 0 56 12"><line x1="0" y1="6" x2="54" y2="6"/><polyline points="46,1 54,6 46,11"/></svg> {href ? "View" : "Get Started"}</a>
        </div>
        <a class="card media work-link" href={href || "#"}>
          <div class={`media-placeholder ${variant}`}></div>
        </a>
      </Fragment>
    ) : (
      <Fragment>
        <a class="card media work-link" href={href || "#"}>
          <div class={`media-placeholder ${variant}`}></div>
        </a>
        <div class="copy">
          <div class="kicker">Work</div>
          <h3>{href ? <a href={href}>{title}</a> : title}</h3>
          <p class="section-desc">{description}</p>
          <a class="arrow" href={href || "#contact"}><svg viewBox="0 0 56 12"><line x1="0" y1="6" x2="54" y2="6"/><polyline points="46,1 54,6 46,11"/></svg> {href ? "View" : "Get Started"}</a>
        </div>
      </Fragment>
    )}
  </div>
</section>
