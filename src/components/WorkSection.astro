---
interface Props {
  title: string;
  description: string;
  variant: string;
  flip?: boolean;
  href?: string;
}

const { title, description, variant, flip = false, href } = Astro.props;
---

<section class="section">
  <div class:list={["grid", "two", { flip }]}>
    {flip ? (
      <Fragment>
        <div class="copy">
          <div class="kicker">Work</div>
          <h3>{href ? <a href={href}>{title}</a> : title}</h3>
          <p class="section-desc">{description}</p>
          <a class="arrow" href={href || "#contact"}><svg class="arrow-icon" viewBox="0 0 40 12" fill="none" xmlns="http://www.w3.org/2000/svg"><line x1="0" y1="6" x2="32" y2="6" stroke="currentColor" stroke-width="1.2"/><path d="M30 1 L38 6 L30 11" stroke="currentColor" stroke-width="1.2" fill="none"/></svg><span>{href ? "View Work" : "Get Started"}</span></a>
        </div>
        <a class="card media work-link" href={href || "#"}>
          <div class={`media-placeholder ${variant}`}></div>
        </a>
      </Fragment>
    ) : (
      <Fragment>
        <a class="card media work-link" href={href || "#"}>
          <div class={`media-placeholder ${variant}`}></div>
        </a>
        <div class="copy">
          <div class="kicker">Work</div>
          <h3>{href ? <a href={href}>{title}</a> : title}</h3>
          <p class="section-desc">{description}</p>
          <a class="arrow" href={href || "#contact"}><svg class="arrow-icon" viewBox="0 0 40 12" fill="none" xmlns="http://www.w3.org/2000/svg"><line x1="0" y1="6" x2="32" y2="6" stroke="currentColor" stroke-width="1.2"/><path d="M30 1 L38 6 L30 11" stroke="currentColor" stroke-width="1.2" fill="none"/></svg><span>{href ? "View Work" : "Get Started"}</span></a>
        </div>
      </Fragment>
    )}
  </div>
</section>
